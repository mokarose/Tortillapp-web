@model Tortillapp_web.Pages.Receta.EditModel

<!--for (int i = 0; i < Model.Step.Count; i++)
{
    <div class="container">
        <div class="row align-content-start">
            <div class="col-6 mb-3">
                <label class="form-label">Paso i</label>
                Html.TextBoxFor(r => r.Step[i].StepDescrp, new { class = "form-control"})
            </div>
        </div>
        <a href="#" onclick="$(this).parent().remove()">Eliminar</a>
    </div>
}-->